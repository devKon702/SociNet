package com.example.socinetandroid.interfaces;

import com.example.socinetandroid.model.RoomsActivity;

public interface IRoomActivityListener {
    boolean onLongClick(RoomsActivity roomsActivity);
    void onClick(RoomsActivity roomsActivity);
}
